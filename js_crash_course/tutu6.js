// sync code runs line by line
// async codes runs symultaneously and doesn't block the execution of the code.
// async code runs in the background and once it's done, it will execute the callback function.