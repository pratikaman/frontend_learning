// React is not
// A framework or language. 
// It is a library that is used to build user interfaces.



// Virtual DOM
// In real DOM, when we change anything, the browser has to re-render the entire DOM tree.
// In Virtual DOM, when we change anything, the browser only re-renders the changed part of the DOM tree.

